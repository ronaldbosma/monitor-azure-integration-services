{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run/Debug logic app Workflows",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureLogicAppsStandard.pickProcess}"
    }
  ]
}
