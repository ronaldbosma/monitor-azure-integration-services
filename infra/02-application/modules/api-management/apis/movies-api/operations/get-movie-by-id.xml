<policies>
    <inbound>
        <base />

        <rewrite-uri template="@($"/movies(PartitionKey='movies',RowKey='{Guid.Parse(context.Request.MatchedParameters["id"])}')")" copy-unmatched-params="false" />
    </inbound>

    <backend>
        <base />
    </backend>

    <outbound>
        <base />

        <choose>
            <when condition="@(context.Response.StatusCode == 200)">
                <set-body>@{
                    var body = context.Response.Body.As<JObject>();

                    var movie = new
                    {
                        id = (Guid)body["RowKey"],
                        title = (string)body["Title"],
                        description = (string)body["Description"],
                        year = (int)body["Year"],
                        rating = (double?)body["Rating"]
                    };

                    return JsonConvert.SerializeObject(movie);
                }</set-body>
            </when>
        </choose>
    </outbound>

    <on-error>
        <base />
    </on-error>
</policies>
